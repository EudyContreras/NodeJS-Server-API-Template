import React, { PureComponent } from 'react';
import config from '../../config';

export default class NavbarPadder extends PureComponent {
	
	render() {
		return (
			<header class='nav-padder'></header>
		)
	}
}